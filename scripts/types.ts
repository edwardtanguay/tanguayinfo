export type Project = {
	idCode: string;
	title: string;
	url: string;
	body: string;
};

export type LatestItem = {
	idCode: string;
	whenOccured: string;
	title: string;
	url: string;
	body: string;
};
